# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Database (MongoDB Memory Server will override this)
MONGODB_URI=mongodb://localhost:27017/akoma_apparel_test

# JWT Configuration for tests
JWT_SECRET=test-secret-key-for-testing

# Stripe Configuration for tests (safe test key)
STRIPE_SECRET_KEY=sk_test_example_key_for_testing

# Email Configuration for tests (mocked)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test-user
SMTP_PASS=test-password

# Frontend URL for tests
FRONTEND_URL=http://localhost:3000
